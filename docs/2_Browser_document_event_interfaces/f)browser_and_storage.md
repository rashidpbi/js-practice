## localStorage

```javascript
// local storage:

// localStorage exists until manual removal (even after reload or restart)
// localStorage is not iterable 

// session storage:
// whereas sessionStorage clears as tab is closed

// methods of both are the same


// cookies:
// cookie is small strings of data that can be stored on browser for use cases like session storage and personalisation. 
// Modern browsers block cookies on "file:// " URLs for security reasons. you can run using vs code live server to enable cookies. 
// it has option of expiration which means data is lost unlike local storage where it is permanent. 


//next steps
// cookies
// intro
// 
```